export interface DependenciesConfig {
  marsy_mock_url_with_port: string;
  marsy_launchpad_url_with_port: string;
  marsy_booster_url_with_port: string;
  marsy_mission_url_with_port: string;
  marsy_payload_url_with_port: string;
}
